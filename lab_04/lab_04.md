# Лабораторная работа №4  
## Оконные функции для анализа данных  

**Номер варианта:** 19  

---

# Цель работы

Изучить концепцию оконных функций в SQL и научиться применять их для выполнения сложных аналитических расчетов: ранжирования, вычисления скользящих средних, нарастающих итогов и сравнительного анализа строк без группировки данных.

---

# Задачи

- Разобрать синтаксис оконных функций: `OVER`, `PARTITION BY`, `ORDER BY`, `ROWS` / `RANGE`.
- Научиться использовать функции ранжирования: `ROW_NUMBER()`, `RANK()`, `DENSE_RANK()`, `NTILE()`.
- Освоить функции смещения: `LAG()`, `LEAD()`.
- Применить агрегатные функции в качестве оконных для вычисления накопительных итогов и скользящих статистик.

---

# Индивидуальные задания
## Задание 1  
Присвоить ранг продажам по сумме. Если суммы равны — ранги должны идти подряд `(DENSE_RANK)`.
[Скриншот выполнения]()

## Задание 2
Разделить клиентов на 2 группы `(NTILE)` по дате регистрации: "Старые" и "Новые".
[Скриншот выполнения]

## Задание 3
Скользящее среднее значение широты `(latitude)` последних 5 добавленных клиентов.
[Скриншот выполнения]


# Вывод
В ходе выполнения лабораторной работы были реализованы аналитические запросы с использованием оконных функций SQL.
Было выполнено:
- присвоение ранга продажам по сумме с использованием функции `DENSE_RANK()` и разбиением данных по продуктам `(PARTITION BY product_id)`. При одинаковых суммах ранги идут подряд, для стабильной сортировки дополнительно учитывается `customer_id`
- разделение клиентов на две равные группы по дате регистрации с использованием функции `NTILE(2)` и присвоением меток "Старые" и "Новые". Группировка выполняется на основе порядка даты добавления клиента `(date_added)` и `customer_id` для однозначной сортировки
- расчёт скользящего среднего значения широты `(latitude)` для клиентов с учётом последних 5 добавленных клиентов с использованием оконной агрегатной функции `AVG()` и рамки `ROWS BETWEEN 4 PRECEDING AND CURRENT ROW`. Результат отсортирован по дате добавления и ограничен 10 записями для наглядности

Файл [lab_04 чумаков.sql](lab_04 чумаков.sql) содержит чистый код выполненных запросов.
